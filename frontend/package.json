{
  "name": "autotest-extension",
  "version": "0.1.0",
  "description": "QA Automation Chrome Extension with AI Agent",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch"
  },
  "dependencies": {
    "@langchain/community": "^0.0.45",
    "@langchain/core": "^0.1.52",
    "langchain": "^0.1.30"
  },
  "devDependencies": {
    "esbuild": "^0.20.1"
  }
}
